#fileModal.modal.fade(tabindex="-1" aria-hidden="true")
	.modal-dialog
		.modal-content
			.modal-header
				button.close(type="button" data-dismiss="modal" aria-label="Close")
					span(aria-hidden="true") ×
			.modal-body.text-center
				img.mw-100
			.modal-footer
				button#btDown.btn.btn-primary(type="button" data-id="")
					i.fas.fa-download
				button.btn.btn-secondary(type="button" data-dismiss="modal") Close
